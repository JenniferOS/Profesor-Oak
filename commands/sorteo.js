var request = require("request");
var { challonge } = require('../config.json')

module.exports = {
    name: 'sorteo',
    description: '',
    execute(message, args) {
        var participants = ['', ''];
    },
};